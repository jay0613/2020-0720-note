set01 = {1,2,3,9,5,42,8,4}
set02 = {"张三",1.5,"李四","王五",2,3}
set03 = {2,3}

# g = set02.pop()  # 删除第一个数字类型元素,并返回该元素
# set01.update(set02)  # 把set02拆了，并把里面的元素添加到set01里
# set01.add("好")	 #为集合添加元素，参数不能是列表，字典，元组，集合
# set01.update({"好"})  # 给集合添加元素,参数可以是列表，字典，元组，集合
# set01.clear()  # 移除集合中的所有元素
# set03 = set01.copy()  # 拷贝一个集合
# set04 = set01.difference(set02)  # 返回set01里比set02多的元素集合
# set01.difference_update(set02)  # 移除两个集合都包含的元素：
# set01.remove(42)  # 移除指定元素,如果元素不存在，会报错
# set01.discard(42)	  # 删除集合中指定的元素,如果元素不存在，不会报错
# set04 = set01.intersection(set02)  # 返回集合的交集
# set01.intersection_update(set02)	  # 除了两个集合的交集，其余元素都删除。
# bool = set01.isdisjoint(set02)  # 判断两个集合是否包含相同的元素，如果没有返回 True，具有相同元素则返回 False。
# bool = set03.issubset(set01)  # 判断set03是set01的子集，是的话返回True，否则false。
# bool = set01.issuperset(set03)  # 判断set03是set01的子集，是的话返回True，否则false。
# set01.symmetric_difference_update(set02)  # 移除当前集合中在另外一个指定集合相同的元素，并将另外一个指定集合中不同的元素插入到当前集合中。
# set04 = set01.symmetric_difference(set02)  # 返回两个集合中不重复的元素集合,和上面的类似。本方法会返回新集合。
# set04 = set01.union(set02)  # 返回两个集合的并集,意思就是两个集合元素全部加起来，然后去重。

# 整理完毕

